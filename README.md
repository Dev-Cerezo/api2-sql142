# api2-sql142

Proyecto Node con estructura de carpetas preparada. Configurar variables en `.env.development` / `.env.test` antes de conectar a base de datos o servicios externos.

## Uso

```bash
npm install
npm start
```

Para desarrollo con recarga al guardar (Node 18+):

```bash
npm run dev
```
